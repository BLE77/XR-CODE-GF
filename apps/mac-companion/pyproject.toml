[project]
name = "xr-agent-mac-companion"
version = "0.1.0"
description = "Mac-side Hermes companion for XR coding workflows"
requires-python = ">=3.11"
dependencies = [
  "websockets>=12.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
