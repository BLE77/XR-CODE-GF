# Streaming Assets

Place runtime-loaded avatar assets here for the native Quest client.

- `Avatars/Yuki.vrm` is the temporary embodied shell while the native import/runtime pipeline is being finalized.
- Future imports can live in `Avatars/` and be referenced by the Quest runtime bootstrap.
